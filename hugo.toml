baseURL = 'https://businessschoolsjobs.github.io/'
languageCode = 'en-us'
title = 'Faculty Jobs in Business Schools - Business Schools Jobs'

[module]
    [[module.imports]]
        path = "university"
        disable = false
    [[module.imports]]
        path = "github.com/gohugoio/hugo-mod-bootstrap-scss/v5"
        disable = false

[markup]
    [markup.tableOfContents]
      endLevel = 3
      startLevel = 1
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true # https://jdhao.github.io/2019/12/29/hugo_html_not_shown/
  # [markup.highlight]
  #   codeFences = false # disables Hugo's default syntax highlighting
  [markup.goldmark.parser]
    [markup.goldmark.parser.attribute]
      block = true

[params]

  # Google Fonts
  google_fonts = [
    ["Inter", "300, 400, 600, 700"],
    ["Fira Code", "500, 700"]
  ]

  sans_serif_font = "Inter"     # Default is System font
  secondary_font  = "Inter"     # Default is System font
  mono_font       = "Fira Code" # Default is System font

    [params.footer]
        copyright = "© :YEAR: Business Schools Jobs."
        version   = true # include git commit info in footer

    [params.social]
        github = "businessschoolsjobs/community" # github.com/YOUR_GITHUB_ID
        # twitter = "#"            # twitter.com/YOUR_TWITTER_ID
        # instagram = "#"      # instagram.com/YOUR_INSTAGRAM_ID
        # rss = true                     # show rss icon with link

    [params.docs] # Parameters for the /docs 'template'
        title           = "Business Schools Jobs"                        # default html title for documentation pages/sections

        # pathName        = "docs"                            # path for documentation site | default "docs"

        # themeColor      = "cyan"                            # (optional) - Set theme accent colour. Options include: blue (default), green, red, yellow, emerald, cardinal, magenta, cyan

        darkMode        = true                                # enable dark mode option? default false

        prism           = true                                # enable syntax highlighting via Prism

        # prismTheme      = "lotusdocs"                         # (optional) - Set theme for PrismJS. Options include: lotusdocs (default), solarized-light, twilight, lucario

        # gitinfo
        repoURL         = "https://github.com/articleos/articleos.github.io"  # Git repository URL for your site [support for GitHub, GitLab, and BitBucket]
        repoBranch      = "release"                           # Name of your Git repository branch
        editPage        = true                                # enable 'Edit this page' feature - default false
        lastMod         = true                                # enable 'Last modified' date on pages - default false
        lastModRelative = true                                # format 'Last modified' time as relative - default true

        # ToC
        toc             = false                                # enable table of contents? default is true
        tocMobile       = false                                # enable table of contents in mobile view? default is true
        scrollSpy       = false                                # enable scrollspy on ToC? default is true

        # icons
        sidebarIcons    = true                                # enable sidebar icons? default false
        titleIcon       = false                                # enable front matter icon title prefix? default is false

        # content navigation
        breadcrumbs     = true                                # default is true
        navDesc         = true                                # include front matter descriptions in Prev/Next navigation cards
        navDescTrunc    = 30                                  # Number of characters by which to truncate the Prev/Next descriptions

        listDescTrunc   = 100                                 # Number of characters by which to truncate the list card description
        backToTop       = true                                # enable back-to-top button? default true

        # front matter
        descriptions    = true                                # enable front matter descriptions under content title?

[[menu.primary]]
    name  = "Dashboard"
    url = "/docs/"
    identifier = "docs"
    weight = 10

# [[menu.primary]]
#     name  = "Showcase"
#     url = "/showcase"
#     identifier = "showcase"
#     weight = 20

[[menu.primary]]
    name  = "Community"
    url = "https://github.com/businessschoolsjobs/community"
    identifier = "community"
    weight = 30
